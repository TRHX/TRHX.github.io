<div class="tags-container">
    <% site.tags.forEach((tag) => { %>
        <a class="tag gt-c-content-color-first gt-a-link" 
            href="<%- url_for(tag.path) %>"><i class="fas fa-hashtag"></i> <%= tag.name %></a>
    <% }); %>
</div>
